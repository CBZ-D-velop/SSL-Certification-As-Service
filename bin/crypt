#!/bin/bash

# Crypt all input files
for input_ca_dir in ./../inputs/*; do
    if [ -d "$input_ca_dir" ]; then
        if [ -d "$input_ca_dir" ]; then
            pushd "$input_ca_dir" >/dev/null
            [ -f "./input.yml" ] && ansible-vault encrypt ./input.yml --vault-password-file ./../../.vault.key
            echo "$input_ca_dir: crypted"
            popd >/dev/null
        fi
    fi
done